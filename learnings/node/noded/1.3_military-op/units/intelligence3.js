//Modules are cached after the first require call


const report = {
  timestamp: Date.now(), // Quality: The current time in milliseconds.
  message: "Enemy sighted at the river.",
};

module.exports = report;

//console.log(module);